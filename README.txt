Flashcard Quiz Flutter Project
=============================

How to use:
1. Open this folder in VS Code.
2. Run `flutter pub get` to fetch dependencies.
3. Run `flutter run` to start the app on a connected device or emulator.
